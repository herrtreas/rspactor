* Drop RSpactor.app into your /Application directory

* Install RSpec if you haven't already: 
  sudo gem install rspec
  
* Symlink RSpactor's command-line tool: 
  sudo ln -s /Applications/RSpactor.app/Contents/Resources/rspactor_bin.rb /usr/bin/rspactor